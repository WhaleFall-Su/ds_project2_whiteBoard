package manager;

public class Consts {
    public static String NOT_APPROVE = "enter not approve";
    public static String APPROVE = "approve enter";
    public static int THICKNESS_INDEX = 1;
    /*thickness = Integer.parseInt(record[1]);
                        graphics2D.setStroke(new BasicStroke(thickness));
    red = Integer.parseInt(record[2]);
    green = Integer.parseInt(record[3]);
    blue = Integer.parseInt(record[4]);
    color = new Color(red, green, blue);
                        graphics2D.setColor(color);
    fromX = Integer.parseInt(record[5]);
    fromY = Integer.parseInt(record[6]);
    toX = Integer.parseInt(record[7]);
    toY = Integer.parseInt(record[8]);
                        graphics2D.drawLine(fromX, fromY, toX, toY);*/
    public static int RED_INDEX = 2;
    public static int GREEN_INDEX = 3;
    public static int BLUE_INDEX = 4;
    public static int FROMX_INDEX = 5;
    public static int FROMY_INDEX = 6;


}
